'use strict';

/**
 * attestation-service router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::attestation-service.attestation-service');
